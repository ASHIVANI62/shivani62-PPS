#include<stdio.h>
int main()
{
	int i,j;
	char input,number='A';
	printf("enter a number to be printed in last row: ");
	scanf("%c", &input);
	for (i=1;j<=(input-'A'+1); ++i)
	{
		for (j=1;j<=i;++j)
		{
			printf("%c ", number);
		}
		++number;
		printf("\n");
	}
	return 0;
}
